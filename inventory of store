# Inventory "store the dolar"

# the user can calculator totality of product and quantity of money.

products = input("into name of a product: ")
price = float(input("into price of product: "))
quantity = int(input("into quantity: "))
resultado = price * quantity

print(f"{products} , {price} * {quantity} = {resultado}  ")
if price == input():
    print(f"into again price: ")

the code is a inventory of store of neighborhood, where you can into , the name of products, the price, quantity and costs totally.
